version: "3.8"

name: wlss-local-test

services:
  postgres:
    image: postgres:latest
    env_file:
      - .env
    ports:
      - 127.0.0.1:5433:5432
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
