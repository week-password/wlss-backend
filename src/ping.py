"""Contains mock functions for tests.

The only one purpose of each function here is to to some mock functionality,
to check that unit-tests for these functions works fine.

They are allowed to be deleted once some actual code with unit-tests will be added.
"""


def ping() -> str:
    """Check testing framework and coverage works fine. Remove once real code with tests will come."""
    return "pong"
